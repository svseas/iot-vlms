[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.frontend"

[build.args]
VITE_API_URL = "https://iot-vlms-production.up.railway.app/api/v1"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
